requests>=2.31
beautifulsoup4>=4.12
pyyaml>=6.0
pypdf>=4.0
pytest>=8.0

# Phase 2: menu extraction engine
pdfplumber>=0.11        # direct PDF text extraction
PyMuPDF>=1.24            # renders PDF pages to images for OCR fallback (imported as `fitz`)
Pillow>=10.0              # image handling for OCR
pytesseract>=0.3.10       # OCR wrapper - requires the `tesseract-ocr` system package too
                          # (Windows: https://github.com/UB-Mannheim/tesseract/wiki,
                          #  Ubuntu/GitHub Actions: apt-get install tesseract-ocr tesseract-ocr-deu)

# Analytics: brand/price aggregation workbook (src/pipeline/generate_report.py)
openpyxl>=3.1
